{
  "name": "discord-player",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "author": "Androz2091",
  "scripts": {
    "build": "turbo run build",
    "prepublish": "lerna run prepublish",
    "format": "prettier --write packages/**/*.{js,ts}"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "prettier": "^2.7.1",
    "turbo": "^1.4.3"
  }
}
